---
name: "Docs: Release to GitHub pages"

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/deploy-docs.yaml"
      - "docs/**"

jobs:
  release-docs:
    name: Release documentation
    runs-on: ubuntu-22.04
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
    steps:
      - name: Generate Token
        uses: tibdex/github-app-token@v1
        id: generate-token
        with:
          app_id: ${{ secrets.BOT_APP_ID }}
          private_key: ${{ secrets.BOT_APP_PRIVATE_KEY }}

      - name: Checkout main branch
        uses: actions/checkout@v3
        with:
          token: ${{ steps.generate-token.outputs.token }}
          fetch-depth: 0

      - name: Build docs
        uses: docker://ghcr.io/bjw-s/mdbook:0.4.31@sha256:412c884c51bb74c2a7075b0215e7ae2c2de9c79b40a825f713a228a1e3d36781
        with:
          args: bash -c "cd docs && mdbook build"

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3.9.3
        if: ${{ github.ref == 'refs/heads/main' }}
        with:
          github_token: ${{ steps.generate-token.outputs.token }}
          publish_dir: ./docs/book/html
          user_name: "pepinillo-bot[bot]"
          user_email: '101514388+pepinillo-bot[bot]@users.noreply.github.com'
          commit_message: ${{ github.event.head_commit.message }}
