---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: "home-dev"
  namespace: network
spec:
  secretName: "home-dev-tls"
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "atsbhome.xyz"
  dnsNames:
    - "atsbhome.xyz"
    - "*.atsbhome.xyz"
