cilium_repo_url: https://helm.cilium.io
cilium_version: 1.15.3
cilium_namespace: kube-system
cilium_values: "{{lookup('ansible.builtin.file', '../../../../../kubernetes/apps/kube-system/cilium/app/values.yaml') | from_yaml }}"
