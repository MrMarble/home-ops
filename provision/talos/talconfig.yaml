---
clusterName: ${clusterName}

talosVersion: v1.4.6
kubernetesVersion: v1.27.3
endpoint: https://${clusterEndpoint}:6443
allowSchedulingOnMasters: true

cniConfig:
  name: none

additionalApiServerCertSans:
  - ${clusterEndpointIP}

additionalMachineCertSans:
  - ${clusterEndpointIP}
  - ${clusterEndpoint}

nodes:
  - hostname: kmaster1
    disableSearchDomain: true
    ipAddress: 192.168.1.10
    installDisk: /dev/sda
    controlPlane: true
    inlinePatches:
    patches:
      - &extensions |-
        machine:
          install:
            extensions:
              - image: ghcr.io/siderolabs/i915-ucode:20230310
              - image: ghcr.io/siderolabs/intel-ucode:20230512
      - &network |-
        machine:
          network:
            interfaces:
              - interface: eth0
                dhcp: true
                vip:
                  ip: ${clusterEndpointIP}

  - hostname: kworker1
    disableSearchDomain: true
    ipAddress: 192.168.1.11
    installDisk: /dev/sda
    controlPlane: true
    patches:
      - *extensions
      - *network

  - hostname: kworker2
    disableSearchDomain: true
    ipAddress: 192.168.1.12
    installDisk: /dev/sda
    controlPlane: true
    patches:
      - *network
      - |-
        machine:
          install:
            extraKernelArgs:
              - nomodeset

controlPlane:
  patches:
    - |-
      machine:
        files:
          - op: create
            path: /etc/cri/conf.d/20-customization.part
            content: |
              [plugins]
                [plugins."io.containerd.grpc.v1.cri"]
                  enable_unprivileged_ports = true
                  enable_unprivileged_icmp = true
        kubelet:
          extraArgs:
            feature-gates: CronJobTimeZone=true,GracefulNodeShutdown=true,NewVolumeManagerReconstruction=false
            rotate-server-certificates: "true"
          extraConfig:
            maxPods: 150
        network:
            extraHostEntries:
              - ip: ${clusterEndpointIP}
                aliases:
                  - ${clusterEndpoint}

    - |-
      cluster:
        proxy:
          disabled: true
        extraManifests:
          - https://raw.githubusercontent.com/MrMarble/home-ops/main/provision/talos/integrations/cilium/cilium.yaml
          - https://raw.githubusercontent.com/MrMarble/home-ops/main/provision/talos/integrations/kubelet-csr-approver/kubelet-csr-approver.yaml

    - |-
      - op: remove
        path: /cluster/apiServer/admissionControl

worker:
  patches:
    - |-
      machine:
        kubelet:
          extraArgs:
            feature-gates: CronJobTimeZone=true,GracefulNodeShutdown=true,NewVolumeManagerReconstruction=false
            rotate-server-certificates: "true"
        files:
          - op: create
            path: /etc/cri/conf.d/20-customization.part
            content: |
              [plugins]
                [plugins."io.containerd.grpc.v1.cri"]
                  enable_unprivileged_ports = true
                  enable_unprivileged_icmp = true
    - |-
      - op: add
        path: /machine/nodeLabels
        value:
          node-role.kubernetes.io/worker: "true"
