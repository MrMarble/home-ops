---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: firefly-data-v1
  namespace: home
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 5Gb

  storageClassName: longhorn
