---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mosquitto-data-v1
  namespace: home
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 128Mi

  storageClassName: longhorn
