---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-config-v1
  namespace: media
spec:
  resources:
    requests:
      storage: 2Gi
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
