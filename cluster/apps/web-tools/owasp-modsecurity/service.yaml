---
apiVersion: v1
kind: Service
metadata:
  name: &app owasp-modsecurity
  namespace: web-tools
  labels:
    app.kubernetes.io/name: *app
    app.kubernetes.io/instance: *app
spec:
  selector:
    app.kubernetes.io/name: *app
    app.kubernetes.io/instance: *app
  ports:
    - protocol: TCP
      name: http
      port: 8080
      targetPort: http
