apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minecraft-config-v1
  namespace: default
spec:
  resources:
    requests:
      storage: 100Mi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
